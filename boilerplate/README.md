# App Name

One sentence: what this app does.

## Run

```bash
cp .env.example .env
# edit .env with your API keys
docker compose up
# open http://localhost:3000
```

First launch will ask you to configure settings (if any).

## What it needs

- (list API keys and credentials here)

## Deploy

Push to GitHub → Coolify auto-deploys → set env vars in Coolify UI.
